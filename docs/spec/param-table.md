# ParamTable 格式

> **状态：待设计。**
>
> 参数表是 Pass 1 与 Pass 2 的接缝（决策 Q14），同时是 L2 参数级比对的载体
> （见 `../testing.md`）。
>
> 需要在此规定：
> - 逐帧状态的完整字段集（Live2D 参数 / 部件不透明度 / UI 状态 / 背景 / 特效）
> - 版本号与兼容策略
> - delta + zstd 的编码格式
> - **与游戏 hook dump 的比对协议**——字段对应关系、容差、缺失字段的处理
> - 随机访问索引（Pass 2 的并行与 seek 依赖它）
