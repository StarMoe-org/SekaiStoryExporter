# IR Schema

> **状态：待设计。**
>
> IR 是解析层与编译层之间的接缝（决策 Q4 / Q14），影响 `sse-ir` / `sse-scenario` /
> `sse-timeline` 三个 crate 的全部接口。**应先于任何实现代码定死。**
>
> 需要在此规定：
> - 事件模型与版本号字段
> - `unsupported` 事件的表达（Q10 选仅 2D，未支持内容须保留足够信息以便日后补齐）
> - 分支树的表达与拍平规则
> - 时间量化规则（秒 → 帧号，建议 `round`，一次定死；见 `coordinate-systems.md` 时间基一节）
> - 人工覆盖文件的合并语义
